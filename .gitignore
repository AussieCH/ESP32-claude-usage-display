# PlatformIO build artifacts
.pio

# VSCode local files
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Internal dev/spec notes — not relevant to users of this repo
claude_esp32_dashboard_spec.md

# Old FastAPI backend — superseded by direct HTTPS on the ESP32
backend/

# Secrets — should never exist as files, but block them just in case
secrets.h
*.env
.env*

# Python bytecode (proxy)
__pycache__/
*.pyc
